package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("CLI Dino Game")
	fmt.Println("Press Ctrl+C to exit")
	
	// Basic application entry point
	// Game initialization will be added in subsequent tasks
	
	// For now, just wait for user input to prevent immediate exit
	fmt.Println("Game setup complete. Implementation coming soon...")
	
	// Exit gracefully
	os.Exit(0)
}